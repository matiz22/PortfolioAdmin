import ListSkills from './ListSkills'
import CreateSkill from './CreateSkill'
import EditSkill from './EditSkill'

const Pages = {
    ListSkills: Object.assign(ListSkills, ListSkills),
    CreateSkill: Object.assign(CreateSkill, CreateSkill),
    EditSkill: Object.assign(EditSkill, EditSkill),
}

export default Pages