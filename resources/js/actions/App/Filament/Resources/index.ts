import Certifications from './Certifications'
import Education from './Education'
import Jobs from './Jobs'
import MailEntries from './MailEntries'
import Proficiencies from './Proficiencies'
import Projects from './Projects'
import Realizations from './Realizations'
import Skills from './Skills'
import SocialLinks from './SocialLinks'
import Tags from './Tags'
import Technologies from './Technologies'
import Users from './Users'

const Resources = {
    Certifications: Object.assign(Certifications, Certifications),
    Education: Object.assign(Education, Education),
    Jobs: Object.assign(Jobs, Jobs),
    MailEntries: Object.assign(MailEntries, MailEntries),
    Proficiencies: Object.assign(Proficiencies, Proficiencies),
    Projects: Object.assign(Projects, Projects),
    Realizations: Object.assign(Realizations, Realizations),
    Skills: Object.assign(Skills, Skills),
    SocialLinks: Object.assign(SocialLinks, SocialLinks),
    Tags: Object.assign(Tags, Tags),
    Technologies: Object.assign(Technologies, Technologies),
    Users: Object.assign(Users, Users),
}

export default Resources