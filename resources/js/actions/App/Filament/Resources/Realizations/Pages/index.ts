import ListRealizations from './ListRealizations'
import CreateRealization from './CreateRealization'
import EditRealization from './EditRealization'

const Pages = {
    ListRealizations: Object.assign(ListRealizations, ListRealizations),
    CreateRealization: Object.assign(CreateRealization, CreateRealization),
    EditRealization: Object.assign(EditRealization, EditRealization),
}

export default Pages