import ListMailEntries from './ListMailEntries'
import ViewMailEntry from './ViewMailEntry'

const Pages = {
    ListMailEntries: Object.assign(ListMailEntries, ListMailEntries),
    ViewMailEntry: Object.assign(ViewMailEntry, ViewMailEntry),
}

export default Pages